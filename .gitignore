node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.output
src/routeTree.gen.ts
.vinxi
todos.json
.vscode/
/vault/
/storage/
.env.docker

# Demo data: STEPs are build-time only (input to scripts/prepare-demo-cad.ts).
# We ship GLB + thumbnails; STEPs are regenerated from the archive when refreshing.
demo-data/robot-arm/step/
html/
.gcloudignore

# Test artifacts
test-results/
playwright-report/
playwright/.auth/
coverage/


# Claude Code local enforcement (not committed to repo)
# These provide hard commit-blocking on local machine only
# Other machines still get testing workflow via CLAUDE.md rules
.claude/hooks/
.claude/dev-session.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
